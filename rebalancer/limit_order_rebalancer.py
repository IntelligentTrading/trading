from exchange.exchange import Exchange
from decimal import Decimal
from typing import Dict


def limit_order_rebalancer(exchange: Exchange, weights: Dict[str, Decimal]):
    # TODO: rebalance with limit orders
    pass
